# BrandWatch - Luxury Watch Catalog

Современный React сайт каталога люксовых часов с темным режимом, локализацией на 3 языках и динамичным фоном.

## Стиль и Дизайн

- **Палитра**: Основной фон `#30333A`, акцентный цвет `#B79D9B`, текст `#FFFFFF` и `#E0E0E0`
- **Шрифт**: Montserrat (400 Regular, 700 Bold) с Google Fonts
- **Режим**: Dark Mode Luxe
- **Фон**: Динамичные движущиеся объекты-часы с ассоциацией времени и часов

## Установка и запуск

### Требования
- Node.js 14+
- npm или yarn

### Шаги установки

```bash
# 1. Перейти в папку проекта
cd /Users/rizatkabdybek/Documents/brandWatch

# 2. Установить зависимости
npm install

# 3. Запустить сервер разработки
npm start

# 4. Открыть в браузере
# Автоматически откроется http://localhost:3000
```

### Сборка для производства

```bash
npm run build
```

## 📁 Структура проекта

```
src/
├── components/          # React компоненты
│   ├── Header.jsx      # Шапка сайта с навигацией
│   ├── Header.css
│   ├── FilterSidebar.jsx    # Панель фильтрации
│   ├── FilterSidebar.css
│   ├── SortDropdown.jsx     # Выпадающий список сортировки
│   ├── SortDropdown.css
│   ├── ProductCard.jsx      # Карточка товара
│   ├── ProductCard.css
│   ├── ProductGrid.jsx      # Сетка товаров
│   └── ProductGrid.css
├── pages/
│   ├── Catalog.jsx      # Главная страница каталога
│   └── Catalog.css
├── hooks/
│   └── useProducts.js   # Кастомный хук для фильтрации и сортировки
├── data/
│   └── products.js      # Mock-данные 20 товаров
├── locales/
│   ├── ru.json          # Русский перевод
│   ├── en.json          # Английский перевод
│   └── kz.json          # Казахский перевод
├── i18n.js              # Конфигурация i18next
├── App.jsx              # Главный компонент с роутингом
├── App.css              # Стили App и динамичный фон
└── index.js             # Точка входа
```

## Локализация (i18n)

Сайт поддерживает 3 языка:
- **RU** - Русский (по умолчанию)
- **EN** - Английский
- **KZ** - Казахский

Переключение языков доступно в заголовке сайта.

## 🔧 Функциональность

### 1. Фильтрация товаров (11 фильтров)
- Бренд (Brand)
- Диаметр (Diameter)
- Пол (Gender)
- Форма корпуса (Case Shape)
- Тип механизма (Movement Type)
- Цвет циферблата (Dial Color)
- Материал корпуса (Case Material)
- Стекло (Glass)
- Материал ремешка (Strap Material)
- Тип застежки (Clasp Type)
- Водонепроницаемость (Water Resistance)

### 2. Сортировка товаров (5 вариантов)
- По популярности (Popularity)
- От дешевых к дорогим (Price: Low to High)
- От дорогих к дешевым (Price: High to Low)
- По новизне (Newest)
- Максимальная скидка (Max Discount)

### 3. Товары
- 20 луксовых часов разных брендов
- Каждый товар имеет: цену, скидку, статус "Новое", описание, фото
- Отображение старой и новой цены при наличии скидки
- Адаптивная сетка товаров (Grid)

### 4. Дизайн
- Темный режим (Dark Mode)
- Люксовый стиль оформления
- Плавные анимации и переходы
- Адаптивный дизайн (Desktop, Tablet, Mobile)

## Динамичный фон

Фон содержит анимированные элементы:
- **Вращающиеся часы** разного размера с разной скоростью
- **Линии времени** которые скользят горизонтально
- **Плавающие круги** как символы циклов времени
- **Светящиеся шары** с эффектом пульсации
- **Градиентный фон** темного люкс-стиля

Все анимации выполнены на CSS и создают ощущение движения времени.

## Адаптивность

Сайт полностью адаптивен:
- **Desktop**: Полный интерфейс с боковой панелью фильтров
- **Tablet**: Оптимизированная верстка, компактные фильтры
- **Mobile**: Мобильное меню, скрытые фильтры с кнопкой открытия

## Кастомный хук useProducts

Хук обрабатывает:
1. **Фильтрацию** товаров по всем 11 критериям
2. **Сортировку** по 5 вариантам
3. **Мемоизацию** результатов для оптимизации производительности

```javascript
const filteredProducts = useProducts(products, filters, sortType);
```

## Зависимости

```json
{
  "react": "^18.2.0",
  "react-dom": "^18.2.0",
  "react-router-dom": "^6.20.0",
  "i18next": "^23.7.0",
  "react-i18next": "^13.5.0",
  "react-scripts": "5.0.1"
}
```

## 💡 Советы по использованию

### Добавление новых товаров
Отредактируйте файл `src/data/products.js` и добавьте новый объект в массив `products`.

### Изменение цветовой палитры
Все цвета используются как CSS переменные в файле `src/App.css`:
- `#30333A` - Основной фон
- `#B79D9B` - Акцентный цвет
- `#FFFFFF` - Белый текст
- `#E0E0E0` - Светло-серый текст

### Добавление новых языков
1. Создайте новый файл `src/locales/[language].json`
2. Добавьте язык в конфигурацию `src/i18n.js`
3. Обновите кнопки в `src/components/Header.jsx`

## Браузерная поддержка

- Chrome (latest)
- Firefox (latest)
- Safari (latest)
- Edge (latest)

## Лицензия

MIT



